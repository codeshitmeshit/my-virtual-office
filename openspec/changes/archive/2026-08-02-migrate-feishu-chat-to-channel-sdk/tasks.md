## 1. Baseline and Dependency Foundation

- [x] 1.1 Add characterization tests for the current Feishu Chat App configuration, inbound-worker authentication, private-chat filtering, representative-Agent routing, provider metadata, persistent idempotency, history/ledger projection, outbound operations, worker status, and notification/card-action isolation; record the pre-migration baseline results in the change evidence.
- [x] 1.2 Create the isolated `integrations/feishu-channel-worker` ESM package, pin `@larksuite/channel` to exactly `0.4.0` with a committed npm lockfile, add Node 18+ and dependency preflight checks, and verify missing/incompatible dependencies produce an actionable Chat-only status without breaking VO startup.

## 2. Versioned Worker Protocol

- [x] 2.1 Implement shared Node-side validation and serialization for `vo.feishu-chat.inbound/v1` and `vo.feishu-chat.command/v1`, including request IDs, worker instance identity, strict operation schemas, bounded payloads, stable error categories, and secret-safe structured logging; cover malformed, oversized, unknown, and redaction cases with Node tests.
- [x] 2.2 Implement atomic mode-0600 worker status snapshots with backward-compatible core fields plus transport, PID/parent/instance, heartbeat, SDK connection, reconnect, callback, command, queue, and spool state; verify readers never observe partial JSON during concurrent updates.

## 3. SDK-backed Inbound Worker

- [x] 3.1 Implement the injectable `@larksuite/channel` worker lifecycle with WebSocket handshake, keepalive, reconnect events, parent-liveness shutdown, private-chat policy, full identity extraction from bounded raw metadata, and normalized text/thread/resource envelopes; configure zero-delay single-message batching, no merge-while-busy, and VO-owned stale/idempotency decisions, then verify behavior with a fake channel.
- [x] 3.2 Implement the bounded atomic inbound spool, durable-ack deletion, exponential callback retry, restart replay, 80% pressure state, 1,000-entry/50-MiB hard limit, and disconnect-before-overflow behavior; fault-test callback failure before/after acknowledgement, worker restart, duplicate replay, permissions, and capacity boundaries.
- [x] 3.3 Add Python `inbound/v1` envelope adaptation and versioned durable acknowledgements to the existing authenticated inbound-worker route while retaining legacy body compatibility; verify complete sender identity, source metadata, private/group behavior, representative-Agent routing, same-chat ordering, cross-restart deduplication, and no acknowledgement before required VO records are durable.

## 4. SDK-backed Outbound and Resource Operations

- [x] 4.1 Implement the authenticated loopback Node command server on an OS-assigned port with per-process token validation, 256-KiB body limit, health/readiness state, operation timeouts, and fake-channel handlers for send, reply, add/remove reaction, recall, and classified SDK errors; verify unauthenticated requests have no Feishu effect.
- [x] 4.2 Implement worker-owned streamed resource downloads under `VO_STATUS_DIR/feishu-chat-attachments`, including generated safe names, root/symlink enforcement, 50-MiB limit, MIME/size metadata, and partial-file cleanup; cover valid images/files, traversal, symlink, oversized, interrupted, and unsupported resource cases.
- [x] 4.3 Add the Python command client and Chat transport ports, route Chat App send/receipt/reaction/recall/download calls through the Node worker when selected, retain legacy Python REST helpers only for legacy Chat and notification paths, and verify Agent results/history survive every outbound failure category.

## 5. Supervision, Selection, and Compatibility

- [x] 5.1 Replace broad Chat worker process scanning with an owned-child supervisor that launches either Node or legacy Python, validates PID/parent/instance/executable identity, rotates tokens, stops one implementation before starting the other, handles orphan/stale status safely, and proves two consumers cannot run for the same Chat App credentials.
- [x] 5.2 Add the backward-compatible `transportImplementation` setting and `VO_FEISHU_CHAT_TRANSPORT` override with precedence `environment > saved config > channel-sdk-node`, preserve existing secret masking and management route/UI fields, and verify configuration changes restart only the Chat App worker while representative-Agent, bindings, history, and notification configuration remain unchanged.
- [x] 5.3 Integrate locked worker dependency installation into the supported startup flow, preserve legacy rollback startup, expose actionable dependency/transport status in existing settings and self-test surfaces, and verify fresh install, upgrade with existing configuration, disabled Chat App, missing credentials, and rollback startup paths.
- [x] 5.4 Reconcile chat-window optimistic user messages with authoritative provider history by exact `idempotencyKey`, remove the matching live-layer bubble after acceptance/history recovery, retain distinct same-text requests with different keys, and add focused history-store/UI regression tests for send, refresh, recovery, failure, and attachment metadata.
- [x] 5.5 Extend the normalized history server source to merge only visible Feishu communication events involving the selected representative Agent across the `feishu-dm:*` and active provider conversation namespaces; preserve strict scoping for provider-native and non-Feishu communication rows, exclude delivery-operation rows, and deduplicate by authoritative event ID.
- [x] 5.6 Update Feishu live invalidation handling so SSE `ready`, `message`, and `delivery` refresh the active normalized history entry, with coalescing that avoids redundant concurrent history requests and recovers events missed during reconnect.
- [x] 5.7 Add focused server and history-store regression tests covering cross-conversation Feishu inclusion, unrelated Agent/non-Feishu exclusion, invisible delivery exclusion, pagination, stable ordering, reconnect refresh, and duplicate prevention.
- [x] 5.8 Add a deterministic browser acceptance that opens the representative Agent chat, injects a Feishu self-test event after SSE readiness, proves request/reply bubbles appear without manual refresh, simulates reconnect recovery, and confirms each authoritative message renders once.
- [x] 5.9 Add ChatWindow bottom-follow state: initialize opened/switched conversations at the newest virtual range and physical bottom after asynchronous layout, capture near-bottom intent before history/live mutations, auto-scroll after new events only while following is enabled, and preserve the viewport while the user reads older history.
- [x] 5.10 Extend history-store/static/browser regressions to prove initial bottom placement, automatic following for new `ready`/`message`/`delivery` and provider events, delayed-layout settling, follow re-enable after returning to bottom, and no forced scroll when positioned above the near-bottom threshold.

## 6. Security, Capacity, and Observability Gates

- [x] 6.1 Enforce global callback concurrency 16, per-chat queue depth 20, provider-derived callback timeout capped at 15 minutes, bounded retries, and rate-limited repeated errors; load-test slow Agents and multiple chats to prove deterministic same-chat order and bounded threads, memory, queue, spool, and log volume.
- [x] 6.2 Add end-to-end security tests using canary App Secrets, worker tokens, authorization headers, cookies, credential-bearing proxy URLs, forged callbacks/commands, oversized bodies, and attachment path attacks; verify logs, status, API responses, audit records, and notifier paths contain no sensitive canary.
- [x] 6.3 Add observable counters and stable states for receive, policy reject, spool/replay/ack, Agent dispatch, outbound categories, resource rejection, reconnect, callback/command failure, pressure/full, authentication failure, and orphan exit; verify existing UI status fields remain compatible and repeated failures increment counters without log storms.

## 7. Integrated Regression and Rollout Evidence

- [x] 7.1 Run and fix the focused Node/Python contract suite covering text, identity, thread/reply, resources, duplicates, ordering, all provider kinds, representative-Agent switch, normalized history/ledger/SSE live refresh and reconnect, worker death/restart, and outbound failures; save reproducible commands and results in the change verification evidence.
- [x] 7.2 Run the relevant full regression for Feishu notifications/card actions, meetings, projects, provider chat, optimistic/history reconciliation, cross-conversation Feishu live synchronization, configuration, static/security checks, browser acceptance, and OpenSpec strict validation; document failures, prove notification isolation, and resolve every migration-caused regression before rollout.
- [x] 7.3 Write the operator runbook for dependency installation, transport selection, status interpretation, pressure recovery, upgrade review, legacy rollback, pending-spool reconciliation, and single-consumer checks; perform and record an offline rollout rehearsal starting with the legacy override, switching to Node, injecting failure, and rolling back without history migration.
- [x] 7.4 Perform real-tenant manual acceptance with valid Feishu Chat App credentials for handshake, private text, image/file resource, duplicate, rapid messages, representative-Agent switch, Agent failure, outbound failure, reconnect, process restart, status/UI history, notification isolation, and rollback; record redacted evidence and do not activate the default transport until all activation gates pass.
