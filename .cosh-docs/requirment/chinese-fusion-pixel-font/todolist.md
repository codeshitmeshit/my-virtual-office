# 执行 Todo List

## TODO-001 获取并核验官方字体产物

- 目标：确定并获取 Fusion Pixel Font 官方简体中文比例版 12px 字体。
- 涉及区域：字体资源目录、官方 Release。
- 输入：需求中的字体版本约束、Fusion Pixel Font 官方发布包。
- 输出：项目内自托管字体文件及其校验信息。
- 依赖：无。
- 完成标准：
  - 字体明确为 `zh_hans`、比例模式、12px。
  - 记录上游版本、原始文件名、下载地址和文件校验值。
  - 未使用第三方重新打包或来源不明的字体。
- 关联 checklist：CHK-001、CHK-020、CHK-022。

## TODO-002 归档字体许可证与来源说明

- 目标：满足 OFL-1.1 的嵌入和再分发要求。
- 涉及区域：字体资源目录、第三方许可证文档。
- 输入：上游 `LICENSE-OFL`、字体版本与修改状态。
- 输出：完整许可证文件和字体来源说明。
- 依赖：TODO-001。
- 完成标准：
  - 仓库包含完整 `LICENSE-OFL`。
  - 来源说明包含项目 URL、版本、文件名、使用模式和是否转换/修改。
  - 如发生格式转换或裁剪，记录衍生过程并继续使用 OFL-1.1。
- 关联 checklist：CHK-002。

## TODO-003 建立统一字体资源与 CSS 变量

- 目标：通过单一字体定义和 CSS 变量控制中英文 UI 字体。
- 涉及区域：共享 CSS、字体静态资源。
- 输入：字体文件、现有 `Press Start 2P` 字体规则。
- 输出：`@font-face`、中文字体变量、英文默认字体变量和回退栈。
- 依赖：TODO-001、TODO-002。
- 完成标准：
  - 字体从项目静态路径加载。
  - 使用 `font-display: swap`。
  - 中文通过 `html[lang="zh"]` 切换，英文保持现有字体。
  - 字体加载失败时回退到可读的系统中文字体。
- 关联 checklist：CHK-003、CHK-004、CHK-009、CHK-016、CHK-017。

## TODO-004 接入主办公室中文字体

- 目标：主办公室普通 UI 在中文模式中统一使用 Fusion Pixel Font。
- 涉及区域：`app/style.css`、`app/index.html`、必要的动态 UI 样式。
- 输入：TODO-003 的字体变量。
- 输出：主界面、工具栏、侧栏、菜单、按钮和普通表单的中文字体规则。
- 依赖：TODO-003。
- 完成标准：
  - 主页面普通 UI 使用统一字体变量。
  - 不通过大量重复选择器覆盖可继承字体。
  - 不覆盖当前未提交的汉化修改。
- 关联 checklist：CHK-004、CHK-007、CHK-012、CHK-013、CHK-022。

## TODO-005 适配动态弹窗与内联字体

- 目标：消除 JavaScript 动态创建 UI 中硬编码字体导致的漏用。
- 涉及区域：`app/game.js` 及动态生成的弹窗、菜单和编辑器。
- 输入：现有动态内联样式、TODO-003 的字体变量。
- 输出：改用 CSS 类、继承或 CSS 变量的动态 UI。
- 依赖：TODO-003、TODO-004。
- 完成标准：
  - 设置、代理、会议、技能、项目和聊天相关动态 UI 使用正确字体。
  - 仅普通 UI 字体被替换，代码和技术内容不受影响。
- 关联 checklist：CHK-008、CHK-012、CHK-013。

## TODO-006 接入安装向导字体

- 目标：安装向导在中文模式使用统一字体，英文模式保持原样。
- 涉及区域：`app/setup.html`。
- 输入：TODO-003 的字体定义或共享字体样式。
- 输出：安装向导标题、步骤标题、按钮和普通表单字体规则。
- 依赖：TODO-003。
- 完成标准：
  - 中文字体即时随语言切换。
  - 许可证输入、路径等技术内容保持适当字体。
  - 页面无布局截断。
- 关联 checklist：CHK-005、CHK-008、CHK-010、CHK-014。

## TODO-007 接入模型设置页字体

- 目标：模型与提供商设置页使用统一中文字体策略。
- 涉及区域：`app/models.html`。
- 输入：TODO-003 的字体定义或共享字体样式。
- 输出：标题、标签、按钮和普通控件字体规则。
- 依赖：TODO-003。
- 完成标准：
  - 模型 ID、API Key、URL 等技术字段保持等宽或既有字体。
  - 中英文运行时切换正确。
  - 页面无文本溢出。
- 关联 checklist：CHK-005、CHK-008、CHK-010、CHK-014。

## TODO-008 接入定时任务页字体

- 目标：定时任务页使用统一中文字体策略。
- 涉及区域：`app/cron.html`。
- 输入：TODO-003 的字体定义或共享字体样式。
- 输出：标题、工具栏、任务卡片、弹窗和普通控件字体规则。
- 依赖：TODO-003。
- 完成标准：
  - Cron 表达式、任务 ID、命令和代理技术标识保持合适字体。
  - 中文和英文模式切换无异常。
  - 窄屏布局可操作。
- 关联 checklist：CHK-005、CHK-008、CHK-010、CHK-014、CHK-015。

## TODO-009 接入官网中文字体

- 目标：官网中文模式采用 Fusion Pixel Font，并保持正文可读性。
- 涉及区域：`website/index.html`、`website/styles.css`。
- 输入：TODO-003 的字体资源和语言状态。
- 输出：官网导航、标题、正文和按钮的中文字体规则。
- 依赖：TODO-003。
- 完成标准：
  - 中文页面使用 Fusion Pixel Font。
  - 英文页面保持 Inter 和现有视觉。
  - 代码块继续使用等宽字体。
  - 长篇中文正文在桌面和移动端可读。
- 关联 checklist：CHK-006、CHK-008、CHK-009、CHK-014、CHK-015。

## TODO-010 配置字体缓存与加载策略

- 目标：控制中文字体的首次加载成本和重复请求。
- 涉及区域：静态资源服务、字体引用 URL、必要的预加载标签。
- 输入：字体文件大小、当前静态文件响应策略。
- 输出：版本化字体 URL、合理缓存头和按需加载策略。
- 依赖：TODO-001、TODO-003。
- 完成标准：
  - 字体响应 MIME 正确。
  - 刷新时可命中缓存。
  - 英文模式不应用中文字体，并尽可能不下载字体。
  - 不存在重复字体请求。
- 关联 checklist：CHK-003、CHK-017、CHK-018、CHK-020。

## TODO-011 增加静态自动化检查

- 目标：防止字体资源、许可证或 CSS 引用在后续修改中失效。
- 涉及区域：测试脚本。
- 输入：字体静态路径、许可证路径、语言 CSS 规则。
- 输出：字体资源完整性和引用检查。
- 依赖：TODO-002、TODO-003。
- 完成标准：
  - 自动检查字体和许可证文件存在。
  - 自动检查 CSS 引用路径有效。
  - i18n 完整性、JS 语法和 `git diff --check` 均通过。
- 关联 checklist：CHK-001、CHK-002、CHK-003、CHK-021。

## TODO-012 执行 MCP 桌面端视觉验证

- 目标：验证中文字体实际加载、页面覆盖和桌面布局。
- 涉及区域：主页面、安装、模型、定时任务、官网。
- 输入：已部署的实现、已确认 checklist。
- 输出：computed font-family、Network、Console 和截图验证记录。
- 依赖：TODO-004 至 TODO-011。
- 完成标准：
  - 使用 MCP 完成 CHK-004 至 CHK-014、CHK-017 至 CHK-019。
  - 发现的截断或漏用完成修复并补测。
  - 验证记录写回 checklist 或测试交付说明。
- 关联 checklist：CHK-004、CHK-005、CHK-006、CHK-007、CHK-008、CHK-009、CHK-010、CHK-011、CHK-012、CHK-013、CHK-014、CHK-017、CHK-018、CHK-019。

## TODO-013 执行 MCP 窄屏与失败回退验证

- 目标：验证移动视口和字体加载失败场景。
- 涉及区域：主页面及各独立页面。
- 输入：已部署的实现。
- 输出：390×844 截图、字体阻断后的回退验证记录。
- 依赖：TODO-012。
- 完成标准：
  - 使用 MCP 完成 CHK-015 和 CHK-016。
  - 页面在字体失败时仍可读、可操作。
  - 窄屏不存在阻塞操作的严重溢出。
- 关联 checklist：CHK-015、CHK-016。

## TODO-014 完成回归、性能记录与需求文档更新

- 目标：完成实施收尾，并为人工测试确认提供完整证据。
- 涉及区域：测试记录、需求归档、工作区差异。
- 输入：所有实现和 MCP 验证结果。
- 输出：完整 checklist 结果、字体体积与缓存记录、实现摘要。
- 依赖：TODO-011、TODO-012、TODO-013。
- 完成标准：
  - CHK-001 至 CHK-022 均有通过/失败和证据记录。
  - 自动检查全部通过。
  - 工作区原有修改未被覆盖。
  - `status.json` 推进到 `implementation_done` 或 `tested` 前遵守人工确认流程。
- 关联 checklist：CHK-001、CHK-002、CHK-003、CHK-020、CHK-021、CHK-022。
