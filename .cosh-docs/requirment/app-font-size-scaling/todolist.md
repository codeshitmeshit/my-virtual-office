# App Font Size Scaling Todolist

## TODO-001: 接入个人字体缩放偏好模型

- 目标：为主应用增加当前浏览器/当前用户的 `fontScale` 展示偏好。
- 涉及区域：`app/game.js`、`app/index.html` 中页面启动脚本、现有 `vo-display-prefs` 读写逻辑。
- 输入：产品澄清结果；现有 `vo-display-prefs`；允许档位 `1`、`1.1`、`1.2`、`1.3`、`1.5`。
- 输出：统一的读取、归一化、应用和保存逻辑；非法值回退到合法值。
- 依赖：无。
- 完成标准：页面启动时能读取本地 `fontScale` 并设置全局缩放状态；非法值不导致报错或白屏。
- 关联 checklist：CHK-002、CHK-009、CHK-011。

## TODO-002: 在设置面板增加 UI 字体大小控件

- 目标：让用户能在设置面板中看到并调整个人 UI 字体大小。
- 涉及区域：`app/index.html` 设置面板；`app/game.js` 的 `_mmLoadCurrentSettings()` 和 `mmSaveSettings()`。
- 输入：五个固定档位 `100%`、`110%`、`120%`、`130%`、`150%`；中英文文案。
- 输出：设置控件、默认值加载、变更事件、保存/持久化行为。
- 依赖：TODO-001。
- 完成标准：设置面板显示 `UI font size` / `界面字体大小`；默认 `100%`；切换档位即时应用；刷新后保留。
- 关联 checklist：CHK-001、CHK-002、CHK-003、CHK-009、CHK-014。

## TODO-003: 增加中英文本地化文案

- 目标：为字体大小设置补齐英文和中文文案，并避免误导用户以为 canvas 文字也会缩放。
- 涉及区域：`app/locales/en.json`、`app/locales/zh.json`，必要时涉及设置说明文字所在 HTML。
- 输入：产品边界“DOM UI only”；控件标签和提示文案。
- 输出：英文 `UI font size`、中文 `界面字体大小` 及必要说明文案。
- 依赖：TODO-002。
- 完成标准：英文和中文界面均有正确文案；文案不承诺“所有文字”或 canvas 字体缩放。
- 关联 checklist：CHK-001、CHK-014、CHK-015。

## TODO-004: 建立全局 DOM UI 字体缩放 CSS 机制

- 目标：用统一机制让主要 DOM UI 字体按比例缩放。
- 涉及区域：`app/style.css`、`app/projects.css`，必要时涉及其他主应用 CSS。
- 输入：全局缩放变量；现有固定字号规则。
- 输出：可复用 CSS 变量或等价机制，例如 `--vo-font-scale` 与关键字号规则的 `calc(...)`。
- 依赖：TODO-001。
- 完成标准：设置面板、工具栏、主要弹窗、聊天、项目、会议等主要 DOM UI 的字体可随 `fontScale` 变化。
- 关联 checklist：CHK-003、CHK-004、CHK-005、CHK-006、CHK-007、CHK-008。

## TODO-005: 覆盖设置面板、工具栏和基础弹窗区域

- 目标：确保主入口和基础操作区域在缩放后可读且可用。
- 涉及区域：`app/style.css`、`app/index.html` 中设置面板和工具栏相关 DOM，必要时涉及动态弹窗 class。
- 输入：现有设置面板、工具栏和弹窗字号。
- 输出：关键区域字号接入缩放机制，必要时调整滚动或换行以避免溢出。
- 依赖：TODO-004。
- 完成标准：`100%` 到 `150%` 下设置面板、工具栏、主要弹窗无明显文字重叠，关键按钮可点击。
- 关联 checklist：CHK-003、CHK-004、CHK-005、CHK-012、CHK-013。

## TODO-006: 覆盖聊天、项目和会议 DOM 面板

- 目标：确保高频阅读和工作流面板参与字体缩放。
- 涉及区域：`app/style.css` 聊天/会议相关规则、`app/projects.css`、必要时涉及动态生成 DOM 的 class。
- 输入：聊天、项目、会议区域现有字号和布局。
- 输出：这些面板的主要标题、正文、按钮、输入区和状态文本接入缩放机制。
- 依赖：TODO-004。
- 完成标准：聊天、项目、会议面板在 `120%` 下可观察到主要 DOM 文本变大；在 `150%` 下关键操作仍可用。
- 关联 checklist：CHK-006、CHK-007、CHK-008、CHK-012。

## TODO-007: 保持服务器配置隔离

- 目标：确保字体缩放只作为本地个人偏好，不写入全局办公室配置。
- 涉及区域：`app/game.js` 的 `mmSaveSettings()`、`/setup/save` 请求构造、`/vo-config` 验证路径。
- 输入：现有设置保存流程；产品澄清结果。
- 输出：字体缩放保存到 `localStorage` 展示偏好，不进入发送给服务器的配置对象。
- 依赖：TODO-001、TODO-002。
- 完成标准：保存字体设置后 `/vo-config` 不出现字体缩放字段；保存其他设置时不会把 `fontScale` 提交为共享配置。
- 关联 checklist：CHK-009、CHK-010。

## TODO-008: 明确 canvas 非目标边界

- 目标：避免用户或验收者把画布内文字未缩放视为本期缺陷。
- 涉及区域：设置文案、交付说明、必要时 requirement/checklist 测试记录。
- 输入：产品澄清结果“本期不覆盖 canvas 绘制文字”。
- 输出：设置提示或交付说明中明确本期只覆盖 DOM UI。
- 依赖：TODO-003。
- 完成标准：实现和交付说明均不承诺 canvas 字体缩放；验收中 canvas 文字未变大不判失败。
- 关联 checklist：CHK-001、CHK-015。

## TODO-009: 增加自动化或半自动验证

- 目标：用代码级测试降低偏好读写和回退逻辑回归风险。
- 涉及区域：现有 JS 测试体系或可运行的轻量脚本测试；相关测试文件。
- 输入：`fontScale` 允许值、非法值、默认值逻辑。
- 输出：覆盖归一化、默认值、localStorage 读写或配置隔离的测试。
- 依赖：TODO-001、TODO-007。
- 完成标准：本地测试命令可运行并通过；至少覆盖默认值和非法值回退。
- 关联 checklist：CHK-002、CHK-009、CHK-010、CHK-011。

## TODO-010: 使用 MCP 浏览器工具执行最终验收

- 目标：按 checklist 在真实页面中完成最终可视和 DOM 状态验证。
- 涉及区域：MCP 浏览器工具、运行中的主应用页面、设置面板、工具栏、聊天、项目、会议、中文界面、窄屏视口。
- 输入：已实现功能；已确认 checklist。
- 输出：MCP 验收记录，包含打开页面、切换档位、刷新持久化、检查关键面板、检查窄屏和中文界面。
- 依赖：TODO-001 至 TODO-009。
- 完成标准：交付说明或测试记录列出 MCP 工具检查结果；发现的问题已修复或明确记录为非阻塞。
- 关联 checklist：CHK-001、CHK-002、CHK-003、CHK-004、CHK-005、CHK-006、CHK-007、CHK-008、CHK-009、CHK-010、CHK-011、CHK-012、CHK-013、CHK-014、CHK-015、CHK-016。

## TODO-011: 更新需求归档和交付状态

- 目标：保持 `.cosh-docs/requirment/app-font-size-scaling/` 与实际推进一致。
- 涉及区域：`checklist.md`、`status.json`，必要时交付说明。
- 输入：实现结果、测试结果、MCP 验收结果、用户后续确认。
- 输出：测试记录、阶段状态更新，以及后续等待用户确认测试通过和最终 done 的状态。
- 依赖：TODO-010。
- 完成标准：开发完成后 checklist 或交付说明记录测试结果；用户已于 2026-06-17T02:40:45+08:00 确认测试通过并完成。
- 关联 checklist：CHK-016。
