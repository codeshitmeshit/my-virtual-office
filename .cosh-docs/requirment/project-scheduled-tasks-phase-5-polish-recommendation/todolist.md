# 项目定时任务 Phase 5 Todolist

## TODO-001 梳理长期项目字段和推荐入口接入点

- 状态：已完成
- 目标：确认项目创建/编辑表单、项目详情页、项目列表摘要、项目存储中适合接入长期项目标记和推荐入口的位置。
- 涉及区域：`app/projects.js`、`app/project_store.py`、`app/server.py`、项目创建/编辑表单、项目详情定时任务面板。
- 输入：Phase 5 requirement/review/checklist、父需求 Phase 5 范围、Phase 1-4 已实现能力。
- 输出：长期项目字段命名、读写路径、推荐入口渲染位置和复用项目 cron 表单的实现结论。
- 依赖：无。
- 完成标准：明确旧项目默认值、项目 summary/detail API 返回字段、推荐入口显示条件。
- 关联 checklist：CHK-001、CHK-002、CHK-003、CHK-004、CHK-005、CHK-010。

## TODO-002 实现长期项目字段持久化和 API 返回

- 状态：已完成
- 目标：为项目增加轻量长期项目标记，并保证旧项目兼容。
- 涉及区域：项目创建/更新逻辑、项目 markdown 存储、项目 summary/detail API。
- 输入：TODO-001 字段方案。
- 输出：项目可保存和读取长期项目标记，旧项目字段缺失时默认为未标记。
- 依赖：TODO-001。
- 完成标准：创建、编辑、重启/重载后长期项目标记保持一致；旧项目不报错。
- 关联 checklist：CHK-001、CHK-010。

## TODO-003 实现创建/编辑项目表单中的长期项目设置

- 状态：已完成
- 目标：让用户能在创建或编辑项目时明确标记长期项目。
- 涉及区域：项目创建/编辑表单 UI、表单提交、项目状态刷新。
- 输入：TODO-002 长期项目字段。
- 输出：长期项目开关或等价控件，保存后能在项目详情和后端数据中体现。
- 依赖：TODO-002。
- 完成标准：新建和编辑项目都能设置长期项目；默认不改变旧项目行为。
- 关联 checklist：CHK-001、CHK-010。

## TODO-004 实现长期项目推荐入口

- 状态：已完成
- 目标：在长期项目且没有项目定时任务时，引导用户创建项目定时任务。
- 涉及区域：项目详情页、项目定时任务面板、项目 cron 列表状态。
- 输入：长期项目字段、当前项目 `scheduledCronJobs`。
- 输出：推荐提示和入口，点击后复用现有项目定时任务创建表单。
- 依赖：TODO-003。
- 完成标准：长期项目且无项目 cron 时展示；已有项目 cron 时不重复展示；取消表单不会创建 cron。
- 关联 checklist：CHK-002、CHK-003、CHK-004、CHK-005、CHK-017。

## TODO-005 补齐项目定时任务本地化 key

- 状态：已完成
- 目标：将项目定时任务相关硬编码文案补齐到中英文 locale。
- 涉及区域：`app/locales/zh.json`、`app/locales/en.json`、项目定时任务面板、历史 UI、表单、toast、推荐提示、全局定时任务页。
- 输入：现有硬编码文案扫描结果。
- 输出：稳定 i18n key 和中英文翻译。
- 依赖：TODO-001。
- 完成标准：新增/相关项目定时任务 UI 在中英文模式下无明显中文硬编码混入英文 UI。
- 关联 checklist：CHK-006、CHK-007、CHK-008、CHK-009。

## TODO-006 替换前端硬编码项目定时任务文案

- 状态：已完成
- 目标：让项目详情页和全局定时任务页使用 locale 文案。
- 涉及区域：`app/projects.js`、`app/cron.html`、必要 CSS/HTML。
- 输入：TODO-005 i18n key。
- 输出：项目定时任务面板、执行历史、创建/编辑表单、toast、推荐入口和全局 cron 项目入口的本地化渲染。
- 依赖：TODO-005。
- 完成标准：中文 UI 保持原有语义；英文 UI 不显示中文项目定时任务文案。
- 关联 checklist：CHK-006、CHK-007、CHK-008、CHK-009。

## TODO-007 补充服务端和存储自动化测试

- 状态：已完成
- 目标：覆盖长期项目字段持久化、旧项目兼容和项目 summary/detail 返回。
- 涉及区域：`tests/`、项目存储测试、项目 API 辅助测试。
- 输入：TODO-002 实现。
- 输出：新增或扩展测试文件。
- 依赖：TODO-002。
- 完成标准：测试覆盖新建长期项目、编辑长期项目、旧项目默认未标记、字段持久化。
- 关联 checklist：CHK-001、CHK-010、CHK-011。

## TODO-008 补充前端和行为回归测试

- 状态：已完成
- 目标：覆盖推荐入口不会自动创建 cron、已有 cron 不重复推荐、普通 Agent cron 不受影响。
- 涉及区域：项目定时任务测试、前端静态检查、必要的端到端或 API smoke test。
- 输入：TODO-004、TODO-006 实现。
- 输出：自动化测试或脚本化验收记录。
- 依赖：TODO-004、TODO-006、TODO-007。
- 完成标准：推荐入口行为、普通 Agent cron 兼容和项目 cron 创建流程有测试或可复现验收。
- 关联 checklist：CHK-002、CHK-003、CHK-004、CHK-005、CHK-012、CHK-017。

## TODO-009 执行 Phase 1-4 回归测试

- 状态：已完成
- 目标：确认 Phase 5 未破坏已归档项目定时任务能力。
- 涉及区域：Phase 1 测试、Phase 2+3 测试、Phase 4 测试、项目执行测试、Cron Manager。
- 输入：已完成实现和现有测试集。
- 输出：回归测试结果记录。
- 依赖：TODO-007、TODO-008。
- 完成标准：项目 cron 绑定、配置、派发、历史、异常提示、普通 Agent cron 回归通过。
- 关联 checklist：CHK-012、CHK-013、CHK-014、CHK-015。

## TODO-010 8090 live 端到端验收

- 状态：已完成
- 目标：在真实运行环境验证 Phase 5 完整体验。
- 涉及区域：8090 本地服务、项目创建/编辑、项目详情、全局定时任务页、项目历史和控制面板。
- 输入：实现完成后的本地服务。
- 输出：live 验收记录。
- 依赖：TODO-009。
- 完成标准：创建长期项目、接受推荐创建项目 cron、取消推荐不创建 cron、run-now 写历史、暂停/恢复、全局可见性符合预期。
- 关联 checklist：CHK-016、CHK-017。

## TODO-011 更新需求归档状态和测试记录

- 状态：已完成
- 目标：把实现、测试、8090 live 验收和人工确认结果回写到 Phase 5 归档。
- 涉及区域：`checklist.md`、`status.json`、必要时 `review.md`。
- 输入：TODO-007 至 TODO-010 的结果。
- 输出：测试记录、阶段推进和等待用户确认记录。
- 依赖：TODO-010。
- 完成标准：开发完成后进入 `tested` 或等待用户确认的正确状态；最终必须由用户确认 done 后归档。
- 关联 checklist：CHK-013、CHK-014、CHK-015、CHK-016、CHK-017。
