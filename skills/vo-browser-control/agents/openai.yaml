interface:
  display_name: "Virtual Office 浏览器状态"
  short_description: "读取 VO 浏览器状态，并在权限阻塞时请求用户介入"
  default_prompt: "<skill_default_prompt><use_skill>$vo-browser-control</use_skill><goal>Inspect the Virtual Office shared browser status, tabs, and controller.</goal><boundary>Request user intervention for login, permission, captcha, MFA, payment, or sensitive-action blockers without taking unsupported browser actions.</boundary></skill_default_prompt>"
