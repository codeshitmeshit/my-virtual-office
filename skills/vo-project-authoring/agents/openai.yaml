interface:
  display_name: "Virtual Office 项目创作"
  short_description: "自然语言确认后直接创建未运行的完整 VO 项目"
  default_prompt: "<skill_default_prompt><trigger>When the user asks to create, reuse, or schedule a VO project.</trigger><use_skill>$vo-project-authoring</use_skill><workflow>Present a natural-language project proposal, wait for confirmation, and then create the unstarted VO project.</workflow></skill_default_prompt>"
policy:
  allow_implicit_invocation: true
